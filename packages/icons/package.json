{
  "name": "@valguide/icons",
  "version": "1.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf src dist node_modules .turbo",
    "build": "svgr --icon --title-prop --typescript --no-dimensions --filename-case kebab --no-index -d dist -- assets"
  },
  "dependencies": {
    "@svgr/cli": "^8.1.0"
  }
}
