export const createInitialUsername = (email: string): Promise<string> => {
  // TODO: implement logic to create unique username based on email address
  //  and append number suffix (use regex to match existing usernames and increment suffix
  throw new Error('Not Implmeneted')
}
