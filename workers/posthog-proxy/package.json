{
  "name": "@valguide/posthog-proxy",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm --dir ../.. wrangler-config generate worker:posthog-proxy --target-env prod && wrangler deploy --config wrangler.generated.jsonc",
    "deploy:dev": "pnpm --dir ../.. wrangler-config generate worker:posthog-proxy --target-env dev && wrangler deploy --config wrangler.generated.jsonc",
    "cf:deploy": "pnpm deploy",
    "cf:deploy:dev": "pnpm deploy:dev"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
