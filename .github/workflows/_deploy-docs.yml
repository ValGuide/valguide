name: Deploy Docs (reusable)

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
    outputs:
      url:
        value: ${{ jobs.deploy-prod.outputs.url || jobs.deploy-dev.outputs.url }}
      environment-name:
        value: ${{ jobs.deploy-prod.outputs.environment-name || jobs.deploy-dev.outputs.environment-name }}

jobs:
  deploy-prod:
    if: inputs.environment == 'prod'
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.info.outputs.url }}
      environment-name: ${{ steps.info.outputs.environment-name }}
    environment:
      name: docs-prod
      url: https://docs.val.guide
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      VITE_ENV: prod
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build & Deploy
        run: pnpm --filter @valguide/docs run deploy
      - id: info
        run: |
          echo "url=https://docs.val.guide" >> $GITHUB_OUTPUT
          echo "environment-name=docs-prod" >> $GITHUB_OUTPUT

  deploy-dev:
    if: inputs.environment == 'dev'
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.info.outputs.url }}
      environment-name: ${{ steps.info.outputs.environment-name }}
    environment:
      name: docs-dev
      url: https://docs-dev.val.guide
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      VITE_ENV: dev
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build & Deploy
        run: pnpm --filter @valguide/docs run deploy:dev
      - id: info
        run: |
          echo "url=https://docs-dev.val.guide" >> $GITHUB_OUTPUT
          echo "environment-name=docs-dev" >> $GITHUB_OUTPUT
