name: Deploy Links (reusable)

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
    outputs:
      url:
        value: ${{ jobs.deploy-prod.outputs.url || jobs.deploy-dev.outputs.url }}
      environment-name:
        value: ${{ jobs.deploy-prod.outputs.environment-name || jobs.deploy-dev.outputs.environment-name }}

jobs:
  deploy-prod:
    if: inputs.environment == 'prod'
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.info.outputs.url }}
      environment-name: ${{ steps.info.outputs.environment-name }}
    environment:
      name: links-prod
      url: https://links.valguide.com
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      VITE_POSTHOG_ENABLED: "true"
      VITE_POSTHOG_KEY: <public-posthog-project-key>
      VITE_POSTHOG_HOST: https://e.valguide.com
      VITE_APP_DOMAIN: app.valguide.com
      VITE_ENV: prod
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build & Deploy
        run: pnpm --filter @valguide/links run deploy
      - id: info
        run: |
          echo "url=https://links.valguide.com" >> $GITHUB_OUTPUT
          echo "environment-name=links-prod" >> $GITHUB_OUTPUT

  deploy-dev:
    if: inputs.environment == 'dev'
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.info.outputs.url }}
      environment-name: ${{ steps.info.outputs.environment-name }}
    environment:
      name: links-dev
      url: https://links.valguide.dev
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      VITE_POSTHOG_ENABLED: "true"
      VITE_POSTHOG_KEY: <public-posthog-project-key>
      VITE_POSTHOG_HOST: https://e.valguide.dev
      VITE_APP_DOMAIN: app.valguide.dev
      VITE_ENV: dev
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build & Deploy
        run: pnpm --filter @valguide/links run deploy:dev
      - id: info
        run: |
          echo "url=https://links.valguide.dev" >> $GITHUB_OUTPUT
          echo "environment-name=links-dev" >> $GITHUB_OUTPUT
